import tempfile
import time 

def core_pipeline(diarized_transcript: str, friend_id: str):
    print("Transcripted and diarized text")
    print(diarized_transcript)
    # throw into Claude 

    # throw into Pinecone
    return